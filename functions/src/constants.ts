/**
 * Application constants
 * Contains constant arrays and configuration values
 */

/**
 * Array of sample names
 * Each sample can be accessed by index or by searching for name
 */
export const samples: string[] = [
  "sample1",
  "sample2",
  "sample3",
  // Add more sample names here as needed
];

/**
 * Discord command names
 */
export const COMMAND_NAMES = {
  SAMPLE: "sample",
} as const;
